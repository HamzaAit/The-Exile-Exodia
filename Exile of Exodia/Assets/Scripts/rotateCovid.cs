using UnityEngine;

public class rotateCovid : MonoBehaviour
{
	public float xAngle, yAngle, zAngle;

	private GameObject covid;
    // Start is called before the first frame update
    void Awake()
    {
        
    }

    // Update is called once per frame
    void Update()
    {
        
    }
}
